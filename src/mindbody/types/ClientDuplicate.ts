export type ClientDuplicates = {
  ClientDuplicates: ClientDuplicate[];
};

export type ClientDuplicate = {
  Id: string;
  UniqueId: string;
  FirstName: string;
  LastName: string;
  Email: string;
};
